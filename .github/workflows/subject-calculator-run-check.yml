name: Subject Calculator Run Check

on:
  push:
    branches:
      - chatgpt-run-check

jobs:
  run-check:
    runs-on: ubuntu-latest
    steps:
      - name: Runner test
        run: |
          echo "Runner is working"
          uname -a
          pwd
